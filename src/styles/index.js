import styled from 'styled-components';
import { Form } from 'reactstrap';

export const FormWrapper = styled(Form)`
    margin-top: .5rem;
    input.form-control {
        width: 350px;
    }
    button {
        background: black;
    }
`